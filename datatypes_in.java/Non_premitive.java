
public class Non_premitive {
    public static void main(String[] arg) {
        // In Java the srins are Immutable
        // Strings- We can write any words sentences.
        // CharAt - We can find the character at any Charcater at any index.
        // length - We can find the lengh of a string
        // repalce - We can repalce the u=ndexes using this function
        // substring - we can extracrt a small string from the main string
        String k = "kapil";
        System.out.println(k.charAt(3));
        System.out.println(k.length());
        String l = k.replace('k', 'p');
        System.out.println(l);
        // syubstring
        String name = "I am pichu the boos ";
        System.out.println(name.substring(3, 8));

    }

}


